import StyleSheet from 'react-native'

const styles = StyleSheet.create({

  button: {
    textAlign: 'center',
    color: '#ffffff',
    marginBottom: 7,
    borderWidth: 2,
    borderColor: blue,
    borderRadius: 2
  }
})

export default styles;
